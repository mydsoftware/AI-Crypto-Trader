import { NextResponse } from "next/server";
import { fetchOkxTickers } from "../../../lib/market";
export const runtime="nodejs";export const dynamic="force-dynamic";
const MAJORS=["BTC/IRT","ETH/IRT","USDT/IRT","BNB/IRT","SOL/IRT","XRP/IRT","DOGE/IRT","ADA/IRT","AVAX/IRT","LINK/IRT","TON/IRT","TRX/IRT","DOT/IRT","LTC/IRT","BCH/IRT"];
export async function GET(){try{const tickers=await fetchOkxTickers();const bySymbol=new Map(tickers.map(t=>[t.symbol,t]));const majors=MAJORS.map(sym=>{const t=bySymbol.get(sym);if(!t)return null;return {symbol:t.symbol,price:t.last,changePct:Math.round(t.changePct*100)/100,quoteVolume:Math.round(t.quoteVolume)};}).filter(Boolean);const top=tickers.slice(0,40).map(t=>({symbol:t.symbol,price:t.last,changePct:Math.round(t.changePct*100)/100,quoteVolume:t.quoteVolume,bid:t.bid,ask:t.ask}));const gainers=[...tickers].sort((a,b)=>b.changePct-a.changePct).slice(0,8);const losers=[...tickers].sort((a,b)=>a.changePct-b.changePct).slice(0,8);return NextResponse.json({updatedAt:new Date().toISOString(),count:tickers.length,majors,symbols:top,gainers:gainers.map(t=>({symbol:t.symbol,changePct:t.changePct,price:t.last})),losers:losers.map(t=>({symbol:t.symbol,changePct:t.changePct,price:t.last})),live:true,exchange:"tabdeal",quote:"IRT",source:"tabdeal-irt"});}catch(e){return NextResponse.json({live:false,exchange:"tabdeal",quote:"IRT",source:"tabdeal-irt",error:String(e),symbols:[],majors:[]},{status:200});}}
